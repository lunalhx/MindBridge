spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/mindbridge?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:mindbridge}
    password: ${DB_PASSWORD:mindbridge}
    driver-class-name: com.mysql.cj.jdbc.Driver
